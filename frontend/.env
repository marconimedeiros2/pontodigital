# Tenant padrão para desenvolvimento local (localhost)
# Em produção este valor é ignorado — o subdomain vem do hostname real
VITE_DEFAULT_TENANT=zico

# Domínio base da aplicação (subdomínios são extraídos daqui)
VITE_BASE_DOMAIN=tempu.com.br
